---
description: MetodiOne Studio landing — project context and 2026-07-14 work
alwaysApply: true
---

# MetodiOne Studio landing (handoff)

## Project
- Path: `/home/koujikin/Projects/MetodiOne`
- Live: https://metodione.com/ (Vercel). Push both `main` and `master`.
- Product: **MetodiOne Studio** — portfolio of custom ops software (not the old somoni SaaS CRM pricing page).
- Backend/CRM still exists; public face is Studio + `/demos` + `/investors`.

## Key files
- `frontend/src/pages/LandingPage.tsx` — home
- `frontend/src/pages/DemoHubPage.tsx`, `InvestorsPage.tsx`, `IndustryPage.tsx`, `ShowcasePage.tsx`
- `frontend/src/content/cases.ts`, `products.ts`, `industries.ts`, `showcases.ts`
- `frontend/src/i18n/landing.ts` — RU/EN copy
- `frontend/src/components/StudioChrome.tsx` — nav/chrome
- Studio styles in `frontend/src/index.css` (`.studio-*`)

## What shipped (2026-07-14 → 07-15)
1. Studio portfolio: bilingual landing, product catalog, demo hub, investors, SEO `/solutions/:slug`, showcases.
2. One-click `/demo` sandboxes for live Amvera apps; hub links use passwordless entry where available.
3. Cases rewritten: context → problem → did → business impact + metrics (no fake +400%).
4. Landing polish commit `07ead25`: brand-led hero, offer strip (discovery → brief → pilot), proof panel, **4 featured cases + expand/collapse** for the rest.
5. Featured case ids: `fuel-wholesale`, `weighbridge-whatsapp`, `crm-service`, `hr-department`.

## Conventions
- Market product names (FuelOps, ScaleGate…), not client codenames.
- Anonymized cases; demos = sandbox DB, never client production creds.
- RU/EN parity in `landing.ts` for every new string.
- Keep first viewport: brand > short offer > 2 CTAs; investors as sublink.
- Do not revert to old ERP/SaaS pricing landing.

## Note
External bots (e.g. Gemini) may cache the old SaaS page; verify with curl/`metodione.com` title: “MetodiOne Studio”.
